
-----------分界线，上述需求已经处理，请忽略------------------
读取简历提取prompt.md文件内容，生成新的功能需求，并添加到demand_tree.md中。
在CHANGELOG_TALK_UTILITY.md记录日志。

表格初始数据从哪里来？页面加载时表格是空的，还是从 teacherrecord.com 的列表接口自动加载数据？

自动加载（页面加载时自动从 list_act 接口拉取 unprocessed 列表）                                                                                                                                                                                                                                               
页面加载时调用 list_act 接口获取待处理教师列表，自动填充 teacherId 列，用户再逐行/全部提取详细信息

-----------分界线，上述需求已经处理，请忽略------------------
进行如下优化：
1. cokie输入框新增标题，新增提示语，如：Cokie: "请输入Cookie"，输入框高度增加两倍。删除保存按钮，自动保存。
2. 表格上的“新增行”改为新增5行；在表格最后一行后最下方新增“新增行”的虚行，点击后新增一行。
3. 简历链接不要省略，其他所有的内容都不能省略。
4. 有无教资内容，鼠标悬浮在上面时，展示$teacherInfo.certificationNameList内容；
5. 新增列，“备注（输入）”：为用户输入的内容。原“备注”列改为备注（最终结果），内容 = $备注（输入） + + “\n” + $teacherInfo.teachingExperience; 
页面列顺序如下：
```
teacherId
备注（输入）
备注（最终结果）
沟通渠道
名字
语种
国家
邮箱
简历链接
来源
网站沟通执行人
初选通过日期
有无教资
是否需邮件提醒添加WS/Teams
邮件提醒日期
邮件执行人
WA/Teams沟通执行人
WA/Teams添加日期
WA/Teams账号
是否需邮件催发checklist/self-intro
邮件催发日期
邮件执行人
WA/Teams及问卷确认最大开课时长
WA/Teams及问卷确认开课时段
特殊备注
是否约面试
不约面试原因
面试时间

```
6. 初选通过日期：默认值：今天，格式yyyy-MM-dd
7. 操作中‘x’改为“删除”按钮，点击时需要弹窗确认是否删除，确认删除成功后，删除行。

-----------分界线，上述需求已经处理，请忽略------------------


问题确认

1. 列顺序：需求里列顺序末尾没有「操作」列，「备注（最终结果）」也不再是固定右列，确认操作（删除）列放在最右侧即可？
放最右侧
2. 备注（最终结果）：是只读展示，还是也支持用户编辑？
支持用户编辑
3. 自动保存 Cookie：是每次输入框内容变化就保存（input 事件），还是失焦（blur）时保存？
   失焦（blur）时保存
-----------分界线，上述需求已经处理，请忽略------------------
1. cookie输入框高度压缩一半以上；
2. 备注（输入）、备注（最终结果）输入框压缩一半以上

-----------分界线，上述需求已经处理，请忽略------------------
1. 在teacherId输入框中回车即触发“提取”逻辑
2. 输入框编辑后，再次提取时，覆盖原有数据
3. “导出excel”按钮移动到最右侧
4. 由于一行太长，“点击新增一行”文字很难被看见，如何优化？

-----------分界线，上述需求已经处理，请忽略------------------
1. 在表格上方左侧，新增按钮“清空数据”（红色），需要用户确认是否清空数据
2. 新增的行，数据应该为空
3. 鼠标悬浮在“无教资”列时，立即展示$teacherInfo.certificationNameList内容
4. 鼠标悬浮在“点击新增一行”时，立即展示“点击新增一行”

-----------分界线，上述需求已经处理，请忽略------------------
1. 未提取时，当前行展示未灰色不可编辑状态；
2. 导出时，teacherId为空的不需要导出。
-----------分界线，上述需求已经处理，请忽略------------------


1. 在表格右上方新增按钮，”导出设置“，点击后在当前页面弹出一个对话框，对话框内容为一个表格，表格结构如下：
标题：列名，是否导出（勾选框）
内容：表格中的每一个列明，默认选中
要求实现的功能：可以拖动每一个列名的顺序，拖动后的顺序需要保存下来；可以新增一列，新增的列导出时默认数据为空。支持在下方新增列、在上方新增列、删除列，在最后新增列。点击保存时，要保存的顺序，下次启动时，依旧生效。
新增的列展示不用同步到表格。
点击导出时，按照新增列的顺序导出。

-----------分界线，上述需求已经处理，请忽略------------------
导出设置中：
1. 整体框变大40%
2. 删除“是否导出”列，勾选框直接合并到“列名”列中
3. 导出设置中：表格内字体变小（参考表格字体大小）； 列名列长度、宽度能容下字就行，尽量小；长度也尽可能小，在页面内能直接展示；按钮字体大小稍微变大；

# 说明
1. 查看并理解TalkUtility项目，在docs/api/生成接口文档
2. 生成的页面风格保持一致，不要乱改样式
3. 已完成的需求追加到demand_tree_completed.md中，不需要修改本文件.
4. 忽略最后出现的“-----------分界线，上述需求已经处理，请忽略------------------”字样以上的需求
5. 请先给出你的理解，有问题提出来。